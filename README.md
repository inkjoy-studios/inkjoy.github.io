# Inkjoy Studio GitHub Pages site

This is the static version of the Inkjoy Studio landing page. It has no React runtime or backend and can be published directly from the repository root with GitHub Pages.

## Local preview

Serve this directory with any static file server, for example:

```sh
python -m http.server 8080
```
